
function elevar(num, exponent) {
   // Retorna el valor de "num" elevado al exponente "exponent".
   // Tu código:
   return Math.pow(num, exponent);
}
let base = 2;
elevado = 3;

let resultado = elevar(base, elevado);

console.log(`${base} elevado a ${elevado} es ${resultado}`);            